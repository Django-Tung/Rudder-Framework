---
type: master-prd
created: 2026-09-23
updated: 2026-09-23
---

# 全局主 PRD

> 本文件是项目级需求主记录。它记录跨 REQ 共享的业务规则、术语、已确认的 UI 主风格和待批准的需求拆分。
> 不属于任何阶段，不设置 `status` / `phase`。REQ 的状态与索引由各 REQ 的 `README.md` 和自动索引维护。

## 1. UI 主风格

- 状态：`UNCONFIRMED`
- 风格名称：待用户确认
- 风格描述：待用户确认
- 确认日期：—
- 适用范围：全项目；单个 REQ 如需例外，必须在对应 `plan.md` 记录原因并经人工确认。

> 首次执行 `/rudder-import` 或 `/rudder-plan` 时必须向用户确认主风格。后续入口必须展示当前风格并询问是否沿用或调整，未确认不得写入正式 PRD。

## 2. 全局业务规则

> 仅记录已由用户确认、且跨多个 REQ 共享的规则；候选项先放在入口的澄清问题中。

- —

## 3. 术语表

> 仅记录已确认的项目术语及其定义。

- —

## 4. 待批准的需求拆分

> `/rudder-import` 在用户确认 UI 主风格和澄清问题后，将候选拆分写入此处。拆分批准后，条目移除并由 REQ 自动索引承接。

```yaml
pending_maps: []
```

## 5. STALE 裁决记录

> 跨 REQ 需求变更的人工裁决摘要。详细证据仍保留在对应 REQ 的 `README.md`。

| 日期 | 触发源 | 裁决结论 | 依据 |
|---|---|---|---|
| — | — | — | — |

## 6. 需求索引

<!-- BEGIN:AUTO-INDEX -->
| 需求ID | 名称 | 状态 | Phase | 依赖 | 来源 | 路径 |
|------|------|------|------|------|------|------|
<!-- END:AUTO-INDEX -->
